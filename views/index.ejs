<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link rel="stylesheet" href="css/style.css">
</head>
<body>
    <h1>Buildings</h1>
    <ul>
    <% for(let i = 0; i < zebra.length; i++) { %>
        <li class='building'>
            <% if(zebra[i].completed === true) {%>
            <span class="completed"><%= zebra[i].building %></span>
            <% }else{ %>
            <span><%= zebra[i].building %></span>
            <% } %>   
            <span class='del'> Delete </span>
        </li>
    <% } %>    
    </ul>

    <h2>Current buildings: <%= left %></h2>

    <form action="/createBuilding" method='POST'>
        <input type="text" placeholder="Building Name" name='buildingName'>
        <input type="text" placeholder="Rooms" name='buildingRooms'>
        <input type="submit">
    </form>

    <script src="js/main.js"></script>
</body>
</html>